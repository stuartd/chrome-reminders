{
  "name": "chrome-meeting-reminders",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.mjs",
    "configure": "node scripts/configure.mjs"
  },
  "engines": { "node": ">=20" }
}
